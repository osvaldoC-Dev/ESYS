[project]
name = "esys-detector"
version = "0.1.0"
description = "ESYS core detector: secrets + PII detection and policy decisions"
requires-python = ">=3.11"

[tool.black]
line-length = 100

[tool.pytest.ini_options]
testpaths = ["esys_detector", "eval"]
